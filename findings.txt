1. Appendix B (forest proof) is invalid as written. The 5-term display isn't a lattice chain — its π-closure is never totally ordered (the wrap-around needs A_n♯ ⊆ π^{2n−1}A_n, impossible), and the argument never uses the loop's closing edge, so it would equally "prove" no geodesics of length ≥ 3 exist. A complete elementary repair is written and machine-verified: unique midpoint (big lattice forced = S + S′) plus forced first step (= Λ_g + π^{n−1}Λ_h via the pr:chain Cartan basis) ⇒ unique geodesics ⇒ no cycles. Robustness bonus: the main theorem needs connectedness and bipartiteness but not pr:forest.
2. U₃(O_F) ⊆ ⟨H,S,R⟩ is load-bearing and unproven — and citing Kalra Cor 5.8 would be circular. ⟨H², S, R⟩ has order only 72; the length-7 word T = HSHHHSH = diag(ω,1,ω)P₁₃ closes the gap, generating all 1296.
3. Line 663: [Λ:Λ♯] = N(det g)⁻¹ is off by a square (correct: 3^{−2v_π(det g)}); the even-dimension remark after Lemma 2.11 is wrong for dim 2 (the standard form is anisotropic since −1 isn't a norm — failure genuinely starts at dim 4, explicit counterexample constructed); plus smaller fixable gaps in prop:distance, le:diagonals, pr:chain, Appendix C (full list: UNDERSTANDING.md §7).